{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "del": "^6.0.0",
    "gh-pages": "^3.1.0",
    "lerna": "^4.0.0",
    "gulp": "^4.0.2",
    "gulp-copy": "^4.0.1",
    "gulp-inject": "^5.0.5",
    "gulp-replace": "^1.1.3"
  },
  "scripts": {
    "build": "lerna run build",
    "build:react": "lerna run build --scope=react-demo",
    "build:vue": "lerna run build --scope=vue-demo",
    "handle": "node ./scripts/index.js",
    "deploy": "yarn run build && yarn run handle && gh-pages -d docs"
  }
}
